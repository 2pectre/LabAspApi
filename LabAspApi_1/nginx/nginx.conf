worker_processes auto;

events {
    worker_connections 1024;
}

http {
upstream labaspapi_dddd{server labaspapi_dddd_green:5000;}
upstream labaspapi_eeee{server labaspapi_eeee_green:5000;}
upstream labaspapi_ffff{server labaspapi_ffff_green:5000;}

    server {
        listen 80;

        server_name servewill.net;

        include /etc/nginx/common/proxy_common.conf;

location /api/dddd/ {proxy_pass http://labaspapi_dddd/;}
location /api/eeee/ {proxy_pass http://labaspapi_eeee/;}
location /api/ffff/ {proxy_pass http://labaspapi_ffff/;}

        location = /favicon.ico {
            log_not_found off;
            access_log off;
            return 204;
        }

        location / {
            deny all;
        }
    }
}
